{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -f package.json ]; then npm install; else cd frontend && npm install; fi",
  "buildCommand": "if [ -f package.json ]; then npm run build; else cd frontend && npm run build && cd .. && rm -rf dist && cp -R frontend/dist dist; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
